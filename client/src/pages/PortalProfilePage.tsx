import { useSchoolAuth } from "@/contexts/SupabaseAuthContext";
import { PortalLayout } from "@/components/PortalLayout";
import { UserRound, ShieldCheck } from "lucide-react";
export default function PortalProfilePage(){const {user,profile}=useSchoolAuth();return <PortalLayout role={profile?.role??"STUDENT"}><main className="mx-auto w-full max-w-4xl px-4 py-6 sm:px-6 lg:px-8"><section className="rounded-3xl border bg-white p-6 shadow-sm sm:p-8"><div className="flex items-center gap-4"><span className="flex h-14 w-14 items-center justify-center rounded-2xl bg-[var(--mist)] text-[var(--accent)]"><UserRound size={26}/></span><div><p className="text-xs font-extrabold uppercase tracking-[.18em] text-[var(--accent)]">My profile</p><h1 className="mt-1 font-serif text-3xl font-semibold text-[var(--ink)]">Account details</h1></div></div><div className="mt-8 grid gap-4 sm:grid-cols-2"><Field label="Full name" value={profile?.display_name||"Not set"}/><Field label="Email" value={user?.email||"Not set"}/><Field label="Role" value={profile?.role||"Not set"}/><Field label="Account status" value={profile?.status||"Not set"}/></div><div className="mt-6 flex items-start gap-3 rounded-2xl bg-slate-50 p-4 text-sm leading-6 text-slate-600"><ShieldCheck className="mt-0.5 shrink-0 text-[var(--accent)]" size={18}/><p>Your access is controlled by the authenticated Supabase profile and database authorization policies. Contact school administration if any account detail is incorrect.</p></div></section></main></PortalLayout>}
function Field({label,value}:{label:string;value:string}){return <div className="rounded-2xl border bg-slate-50 p-4"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">{label}</p><p className="mt-2 font-semibold text-[var(--ink)]">{value}</p></div>}
