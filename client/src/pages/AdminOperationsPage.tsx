import { useCallback, useEffect, useState } from "react";
import { Activity, BarChart3, CalendarDays, CheckCircle2, ClipboardCheck, CreditCard, FileText, GraduationCap, Loader2, Megaphone, RefreshCw, Users } from "lucide-react";
import { useLocation } from "wouter";
import { getSupabase } from "@/lib/supabase";
import { isAdministrator, useSchoolAuth } from "@/contexts/SupabaseAuthContext";

type Metric={label:string;value:string;detail:string;icon:typeof Activity}; const money=(v:number)=>new Intl.NumberFormat("en-KE",{style:"currency",currency:"KES",maximumFractionDigits:0}).format(v);
export default function AdminOperationsPage(){const {user,profile,loading:authLoading}=useSchoolAuth();const [,navigate]=useLocation();const [loading,setLoading]=useState(true),[error,setError]=useState<string|null>(null),[metrics,setMetrics]=useState<Metric[]>([]);
 const load=useCallback(async()=>{if(!user||!profile||!isAdministrator(profile.role))return;setLoading(true);setError(null);try{const db=getSupabase();const year=new Date().getFullYear();const today=new Date().toISOString().slice(0,10);const [s,a,as,e,n,p,i]=await Promise.all([db.from("students").select("id",{count:"exact",head:true}).eq("status","ACTIVE"),db.from("admission_applications").select("id,status"),db.from("attendance_sessions").select("id").eq("session_date",today),db.from("events").select("id",{count:"exact",head:true}).gte("starts_at",`${year}-01-01T00:00:00+03:00`).lte("starts_at",`${year}-12-31T23:59:59+03:00`),db.from("announcements").select("id",{count:"exact",head:true}).eq("status","PUBLISHED"),db.from("payments").select("amount,status").eq("status","CONFIRMED"),db.from("invoices").select("id,status")]);for(const r of [s,a,as,e,n,p,i])if(r.error)throw r.error;const pending=(a.data??[]).filter(r=>["submitted","pending","under_review"].includes(String(r.status).toLowerCase())).length;const confirmed=(p.data??[]).reduce((sum,r)=>sum+Number(r.amount??0),0);const open=(i.data??[]).filter(r=>!["PAID","CANCELLED"].includes(String(r.status).toUpperCase())).length;setMetrics([{label:"Learners",value:String(s.count??0),detail:"Active student records",icon:GraduationCap},{label:"Admissions queue",value:String(pending),detail:"Applications awaiting review",icon:Users},{label:"Attendance sessions today",value:String(as.data?.length??0),detail:"Class registers opened today",icon:ClipboardCheck},{label:"Published notices",value:String(n.count??0),detail:"Published announcements",icon:Megaphone},{label:`${year} events`,value:String(e.count??0),detail:"Calendar records",icon:CalendarDays},{label:"Confirmed payments",value:money(confirmed),detail:"Confirmed payment value",icon:CreditCard},{label:"Open invoices",value:String(open),detail:"Invoices not marked paid/cancelled",icon:BarChart3}]);}catch(e){setError(e instanceof Error?e.message:"Operations data could not be loaded.");}finally{setLoading(false);}},[profile,user]);
 useEffect(()=>{if(authLoading)return;if(!user||!profile){navigate("/portal/login");return;}if(!isAdministrator(profile.role)){navigate("/portal/parent");return;}void load();},[authLoading,load,navigate,profile,user]);
 if(authLoading||loading)return <main className="grid min-h-[60vh] place-items-center"><div className="flex items-center gap-3 text-sm text-[var(--ink)]/60"><Loader2 className="animate-spin" size={18}/>Loading operations…</div></main>;
 return <main className="mx-auto w-full max-w-[1440px] space-y-6 px-4 py-5 sm:px-6 lg:px-8 lg:py-8"><header className="rounded-[2rem] bg-[var(--ink)] p-6 text-white shadow-xl sm:p-8"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><span className="inline-flex items-center gap-2 rounded-full bg-[var(--gold)]/15 px-3 py-1 text-xs font-bold uppercase tracking-[.16em] text-[var(--gold)]"><Activity size={14}/> Operations</span><h1 className="mt-4 font-serif text-4xl font-semibold">School command & reports</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-white/65">Live operational reporting across the canonical school records.</p></div><button onClick={()=>void load()} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-white/15 bg-white/5 px-4 text-sm font-bold"><RefreshCw size={17}/>Refresh live data</button></div></header>{error&&<div role="alert" className="rounded-2xl bg-red-50 px-4 py-3 text-sm text-red-800">{error}</div>}<section className="grid grid-cols-1 gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(({label,value,detail,icon:Icon})=><article key={label} className="menwe-card rounded-2xl p-5 transition-all duration-300 hover:-translate-y-1 hover:shadow-xl"><div className="flex items-start justify-between gap-3"><span className="grid h-11 w-11 place-items-center rounded-xl bg-[var(--ink)]/5 text-[var(--ink)]"><Icon size={20}/></span><span className="text-right text-2xl font-black">{value}</span></div><p className="mt-5 text-sm font-bold">{label}</p><p className="mt-1 text-xs leading-5 text-[var(--ink)]/55">{detail}</p></article>)}</section><section className="grid gap-4 lg:grid-cols-2"><article className="menwe-card rounded-[1.75rem] p-6"><div className="flex items-center gap-3"><FileText className="text-[var(--accent)]" size={22}/><h2 className="font-serif text-2xl font-semibold">Reporting workspaces</h2></div><div className="mt-5 grid gap-2 sm:grid-cols-2"><button onClick={()=>navigate("/portal/admin/attendance")} className="rounded-xl border p-4 text-left text-sm font-bold hover:bg-[var(--mist)]">Attendance register →</button><button onClick={()=>navigate("/portal/admin/directory")} className="rounded-xl border p-4 text-left text-sm font-bold hover:bg-[var(--mist)]">People & enrolment →</button><button onClick={()=>navigate("/portal/admin/finance")} className="rounded-xl border p-4 text-left text-sm font-bold hover:bg-[var(--mist)]">Finance directory →</button><button onClick={()=>navigate("/portal/admin/content")} className="rounded-xl border p-4 text-left text-sm font-bold hover:bg-[var(--mist)]">Content management →</button><button onClick={()=>navigate("/portal/exams")} className="rounded-xl border p-4 text-left text-sm font-bold hover:bg-[var(--mist)]">Exams & report cards →</button></div></article><article className="menwe-card rounded-[1.75rem] p-6"><div className="flex items-center gap-3"><CheckCircle2 className="text-[var(--accent)]" size={22}/><h2 className="font-serif text-2xl font-semibold">Operational integrity</h2></div><p className="mt-4 text-sm leading-6 text-[var(--ink)]/60">Every metric is read from live Supabase tables. Empty data remains empty; database failures are surfaced rather than replaced with fabricated values.</p><div className="mt-5 rounded-xl bg-[var(--accent)]/10 p-4 text-xs font-semibold leading-5">Production rule: no placeholder counts and no silent schema fallbacks.</div></article></section></main>;
}
