import { Check, FileText, Loader2, Printer, Users } from "lucide-react";
import { Fragment, useEffect, useMemo, useState } from "react";
import { getSupabase } from "@/lib/supabase";
import { useSchoolAuth } from "@/contexts/SupabaseAuthContext";
import { PortalLayout } from "@/components/PortalLayout";

type Row = Record<string, any>;
type SubjectPair = Row & { id?: any; code?: any; name?: any; scId?: any; lId?: any };
const text = (v: unknown, fallback = "—") => v == null || v === "" ? fallback : String(v);
const nameOf = (s: Row) => [s.first_name, s.middle_name, s.last_name].filter(Boolean).join(" ");
const genderCode = (value: unknown) => { const v = String(value ?? "").trim().toUpperCase(); return v === "M" || v === "MALE" ? "M" : v === "F" || v === "FEMALE" ? "F" : ""; };

const printStyles = `
.marksheet-print { border:1px solid rgba(6,18,41,.10); border-radius:1.5rem; box-shadow:0 18px 50px rgba(6,18,41,.08); }
.marksheet-print > header { position:relative; overflow:hidden; border:0 !important; border-bottom:1px solid rgba(216,155,40,.35) !important; border-radius:1.15rem 1.15rem 0 0; padding:1.25rem 1.25rem .9rem !important; background:linear-gradient(135deg,#061229 0%,#102143 72%,#17315b 100%); color:#fff; }
.marksheet-print > header::after { content:""; position:absolute; left:0; right:0; bottom:0; height:3px; background:#D89B28; }
.marksheet-print > header h2 { letter-spacing:.08em; }
.marksheet-print > header p { color:rgba(255,255,255,.72); }
.marksheet-print > header h3 { color:#fff; letter-spacing:.025em; }
.marksheet-print > header p:last-child { color:#D89B28; }
.marksheet-print table { border-color:rgba(6,18,41,.12); table-layout:fixed; }
.marksheet-print thead tr:first-child th { background:#061229; color:#fff; border-color:rgba(255,255,255,.25) !important; font-weight:800; letter-spacing:.025em; }
.marksheet-print thead tr:nth-child(2) th { background:rgba(216,155,40,.16); color:#061229; font-weight:800; }
.marksheet-print tbody tr:nth-child(even) { background:rgba(6,18,41,.025); }
.marksheet-print tbody tr:hover { background:rgba(216,155,40,.09); }
.marksheet-print tbody td { border-color:rgba(6,18,41,.10) !important; }
.marksheet-print tbody td:nth-child(3) { color:#061229; }
.marksheet-print tbody td:nth-last-child(-n+2) { background:rgba(216,155,40,.07); font-weight:800; }
.marksheet-print th,.marksheet-print td { width:28px; padding:.3rem .2rem !important; overflow:hidden; white-space:nowrap; text-overflow:clip; }
.marksheet-print th:nth-child(1),.marksheet-print td:nth-child(1) { width:34px; }
.marksheet-print th:nth-child(2),.marksheet-print td:nth-child(2) { width:88px; }
.marksheet-print th:nth-child(3),.marksheet-print td:nth-child(3) { width:155px; }
.marksheet-print th:nth-child(4),.marksheet-print td:nth-child(4) { width:48px; }
.marksheet-print th:nth-last-child(2),.marksheet-print td:nth-last-child(2) { width:76px; }
.marksheet-print th:last-child,.marksheet-print td:last-child { width:72px; }
.marksheet-print select { max-width:34px; }
@media print {
 @page { size:A4 landscape; margin:5mm; }
 html,body { width:100%; margin:0 !important; padding:0 !important; background:#fff !important; }
 body * { visibility:hidden; }
 .marksheet-print,.marksheet-print * { visibility:visible; }
 .marksheet-print { position:absolute !important; left:0 !important; top:0 !important; width:100% !important; max-width:none !important; min-width:0 !important; margin:0 !important; padding:0 !important; overflow:visible !important; background:#fff !important; border:0 !important; border-radius:0 !important; box-shadow:none !important; -webkit-print-color-adjust:exact !important; print-color-adjust:exact !important; }
 .marksheet-print > header { padding:2.5mm 2mm 2.2mm !important; border-radius:0 !important; background:#061229 !important; color:#fff !important; }
 .marksheet-print > header::after { height:1mm !important; background:#D89B28 !important; }
 .marksheet-print header h2 { margin:0 !important; font-size:13pt !important; line-height:1.05 !important; letter-spacing:.07em !important; }
 .marksheet-print header p { margin:1mm 0 0 !important; font-size:6pt !important; line-height:1.1 !important; }
 .marksheet-print header h3 { margin:1mm 0 0 !important; font-size:8pt !important; line-height:1.1 !important; }
 .marksheet-print table { width:100% !important; min-width:0 !important; table-layout:fixed !important; margin-top:2mm !important; border-collapse:collapse !important; font-size:5.5pt !important; }
 .marksheet-print th,.marksheet-print td { padding:.5mm .3mm !important; line-height:1 !important; height:3.5mm !important; max-height:3.5mm !important; overflow:hidden !important; white-space:nowrap !important; }
 .marksheet-print th:nth-child(1),.marksheet-print td:nth-child(1){width:6mm !important;}
 .marksheet-print th:nth-child(2),.marksheet-print td:nth-child(2){width:18mm !important;}
 .marksheet-print th:nth-child(3),.marksheet-print td:nth-child(3){width:34mm !important;}
 .marksheet-print th:nth-child(4),.marksheet-print td:nth-child(4){width:8mm !important;}
 .marksheet-print th:nth-last-child(2),.marksheet-print td:nth-last-child(2){width:14mm !important;}
 .marksheet-print th:last-child,.marksheet-print td:last-child{width:14mm !important;}
 .marksheet-print thead { display:table-header-group; }
 .marksheet-print tr { break-inside:avoid; page-break-inside:avoid; }
 .marksheet-print tbody tr { height:3.5mm !important; }
 .marksheet-print > p { margin-top:1.5mm !important; font-size:5pt !important; line-height:1 !important; }
 .marksheet-print tbody tr:nth-child(even){background:#fafafa !important;}
 .marksheet-print tbody td:nth-last-child(-n+2){background:#f7f0df !important;}
 .marksheet-print select { appearance:none !important; border:0 !important; background:transparent !important; padding:0 !important; font-weight:800 !important; }
}
`;

export default function ClassMarksheetPage() {
  const { user, profile } = useSchoolAuth();
  const [years,setYears]=useState<Row[]>([]),[terms,setTerms]=useState<Row[]>([]),[classes,setClasses]=useState<Row[]>([]),[subjects,setSubjects]=useState<Row[]>([]),[exams,setExams]=useState<Row[]>([]),[rows,setRows]=useState<Row[]>([]);
  const [yearId,setYearId]=useState(""),[termId,setTermId]=useState(""),[classId,setClassId]=useState(""),[teacherClassIds,setTeacherClassIds]=useState<string[]>([]),[loading,setLoading]=useState(true),[generating,setGenerating]=useState(false),[message,setMessage]=useState<string|null>(null),[savingGenderId,setSavingGenderId]=useState("");
  const admin=["SUPER_ADMIN","ADMIN","HEAD_OF_INSTITUTION","DEPUTY_HOI"].includes(profile?.role??"");

  useEffect(()=>{void(async()=>{if(!user||(!admin&&profile?.role!=="TEACHER"))return;try{const db=getSupabase();if(!admin){const tr=await db.from("teachers").select("id").eq("profile_id",user.id).maybeSingle();if(tr.error)throw tr.error;if(!tr.data)throw new Error("Your teacher record is not linked yet.");const ta=await db.from("teacher_assignments").select("class_id").eq("teacher_id",tr.data.id);if(ta.error)throw ta.error;setTeacherClassIds((ta.data??[]).map(x=>String(x.class_id)));}else setTeacherClassIds([]);const[y,t,c,s]=await Promise.all([db.from("academic_years").select("id,name,starts_on,ends_on,is_current,status").eq("status","ACTIVE").order("starts_on",{ascending:false}),db.from("terms").select("id,academic_year_id,name,starts_on,ends_on,is_current,status").eq("status","ACTIVE").order("starts_on",{ascending:false}),db.from("classes").select("id,academic_year_id,code,name,level,status").eq("status","ACTIVE").order("name"),db.from("subjects").select("id,code,name,status").eq("status","ACTIVE").order("name")]);for(const r of[y,t,c,s])if(r.error)throw r.error;setYears(y.data??[]);setTerms(t.data??[]);setClasses(c.data??[]);setSubjects(s.data??[]);setYearId(String(y.data?.find(x=>x.is_current)?.id??y.data?.[0]?.id??""));}catch(e){setMessage(e instanceof Error?e.message:"Marksheet setup could not be loaded.")}finally{setLoading(false)}})()},[admin,profile?.role,user]);
  const visibleTerms=useMemo(()=>terms.filter(t=>!yearId||String(t.academic_year_id)===yearId),[terms,yearId]);
  const visibleClasses=useMemo(()=>classes.filter(c=>(!yearId||String(c.academic_year_id)===yearId)&&(admin||teacherClassIds.includes(String(c.id)))),[classes,yearId,admin,teacherClassIds]);
  const titleClass=classes.find(c=>String(c.id)===classId); const selectedTerm=terms.find(t=>String(t.id)===termId);

  const generate=async()=>{if(!classId||!yearId||!termId){setMessage("Select academic year, term and class first.");return}setGenerating(true);setMessage(null);try{const db=getSupabase();if(!admin){const tr=await db.from("teachers").select("id").eq("profile_id",user?.id??"").maybeSingle();if(tr.error)throw tr.error;if(!tr.data)throw new Error("Your teacher record is not linked yet.");const ta=await db.from("teacher_assignments").select("class_id").eq("teacher_id",tr.data.id).eq("class_id",classId);if(ta.error)throw ta.error;if(!(ta.data??[]).length)throw new Error("You are not assigned to this class.")};const en=await db.from("enrollments").select("student_id").eq("class_id",classId).eq("academic_year_id",yearId).eq("status","ACTIVE");if(en.error)throw en.error;const ids=[...new Set((en.data??[]).map(x=>String(x.student_id)))];if(!ids.length){setRows([]);setExams([]);setMessage("No active learners are enrolled in this class for the selected academic year.");return};const[s,r,e]=await Promise.all([db.from("students").select("id,admission_number,first_name,middle_name,last_name,gender").in("id",ids).order("first_name").order("last_name"),db.from("exam_results").select("student_id,subject_id,score,maximum_score,grade,exams!inner(id,term_id,class_id,name,exam_type,starts_on,ends_on),subjects(id,code,name)").eq("exams.term_id",termId).eq("exams.class_id",classId).in("student_id",ids),db.from("exams").select("id,name,exam_type,starts_on,ends_on").eq("term_id",termId).eq("class_id",classId).order("starts_on")]);for(const q of[s,r,e])if(q.error)throw q.error;setExams((e.data??[]) as Row[]);const resultRows=(r.data??[]) as Row[];const byStudent=new Map<string,Row>();(s.data??[]).forEach(student=>byStudent.set(String(student.id),{...student,gender:genderCode(student.gender),results:resultRows.filter(x=>String(x.student_id)===String(student.id))}));setRows([...byStudent.values()]);if(!resultRows.length)setMessage("No persisted assessment results exist for this term and class. Empty marks are shown; no scores are invented.");}catch(e){setRows([]);setMessage(e instanceof Error?e.message:"The class marksheet could not be generated.")}finally{setGenerating(false)}};
  const saveGender=async(studentId:string,value:string)=>{const gender=genderCode(value);if(!gender)return;setSavingGenderId(studentId);setMessage(null);try{const r=await getSupabase().from("students").update({gender}).eq("id",studentId);if(r.error)throw r.error;setRows(current=>current.map(row=>String(row.id)===studentId?{...row,gender}:row));setMessage("Gender updated successfully.");}catch(e){setMessage(e instanceof Error?e.message:"Gender could not be updated.")}finally{setSavingGenderId("")}};
  const subjectPairs=useMemo<SubjectPair[]>(()=>subjects.map(s=>{const relevant=exams.filter(e=>String(e.exam_type??"").toUpperCase().includes(String(s.code??"").toUpperCase())||String(e.name??"").toUpperCase().includes(String(s.code??"").toUpperCase())||String(e.name??"").toUpperCase().includes(String(s.name??"").toUpperCase()));const sc=relevant.find(e=>/\bSC\b|SCHOOL|WRITTEN|STRUCTURED/i.test(`${e.name} ${e.exam_type}`));const l=relevant.find(e=>/\bL\b|LISTENING|ORAL|LANGUAGE/i.test(`${e.name} ${e.exam_type}`));return{...s,scId:sc?.id,lId:l?.id}}),[exams,subjects]);
  const scoreFor=(row:Row,subjectId:string,examId?:string)=>{if(!examId)return"";const r=row.results?.find((x:Row)=>String(x.subject_id)===subjectId&&String(x.exams?.id)===String(examId));return r?.score==null?"":String(r.score)};
  const totalFor=(row:Row)=>row.results?.reduce((sum:number,r:Row)=>sum+(Number.isFinite(Number(r.score))?Number(r.score):0),0)??0;
  const examLabel=exams.length?exams.map(e=>text(e.name)).join(" · "):"No assessments recorded";
  const genderComplete=rows.length>0&&rows.every(r=>genderCode(r.gender));
  if(loading)return <PortalLayout role={profile?.role??"TEACHER"}><main className="grid min-h-[60vh] place-items-center"><Loader2 className="animate-spin text-[var(--accent)]"/></main></PortalLayout>;
  return <PortalLayout role={profile?.role??"TEACHER"}><style>{printStyles}</style><main className="mx-auto w-full max-w-[1800px] space-y-6 px-4 py-5 sm:px-6 lg:px-8 lg:py-8">
    <section className="menwe-card overflow-hidden rounded-[1.75rem] border border-[var(--ink)]/10 bg-white shadow-sm print:hidden"><div className="bg-[var(--ink)] px-5 py-6 text-white sm:px-7"><div className="flex flex-wrap items-start justify-between gap-4"><div><span className="inline-flex items-center gap-2 text-xs font-black uppercase tracking-[.16em] text-[var(--gold)]"><FileText size={15}/> Class marksheet</span><h1 className="mt-2 font-serif text-3xl font-semibold">Assessment marksheet</h1><p className="mt-2 max-w-2xl text-sm text-white/65">A clean, print-ready class register using real learner, gender and assessment records from Supabase.</p></div><div className="rounded-2xl border border-white/10 bg-white/5 px-4 py-3 text-right"><div className="text-2xl font-black">{rows.length}</div><div className="text-[10px] font-bold uppercase tracking-wider text-white/55">Learners loaded</div></div></div></div><div className="p-5 sm:p-7"><div className="grid gap-3 md:grid-cols-3"><label className="grid gap-1.5 text-xs font-bold uppercase tracking-wide text-[var(--ink)]/60">Academic year<select value={yearId} onChange={e=>{setYearId(e.target.value);setTermId("");setClassId("")}} className="min-h-12 rounded-xl border border-[var(--ink)]/15 bg-white px-3 text-sm font-semibold normal-case tracking-normal"><option value="">Academic year</option>{years.map(y=><option key={text(y.id)} value={text(y.id)}>{text(y.name)}</option>)}</select></label><label className="grid gap-1.5 text-xs font-bold uppercase tracking-wide text-[var(--ink)]/60">Term<select value={termId} onChange={e=>setTermId(e.target.value)} className="min-h-12 rounded-xl border border-[var(--ink)]/15 bg-white px-3 text-sm font-semibold normal-case tracking-normal"><option value="">Term</option>{visibleTerms.map(t=><option key={text(t.id)} value={text(t.id)}>{text(t.name)}</option>)}</select></label><label className="grid gap-1.5 text-xs font-bold uppercase tracking-wide text-[var(--ink)]/60">Class<select value={classId} onChange={e=>setClassId(e.target.value)} className="min-h-12 rounded-xl border border-[var(--ink)]/15 bg-white px-3 text-sm font-semibold normal-case tracking-normal"><option value="">Class</option>{visibleClasses.map(c=><option key={text(c.id)} value={text(c.id)}>{text(c.name)}</option>)}</select></label></div><div className="mt-4 flex flex-wrap items-center gap-3"><button type="button" onClick={()=>void generate()} disabled={generating||!classId||!termId} className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-[var(--ink)] px-5 text-sm font-bold text-white shadow-sm transition hover:-translate-y-0.5 hover:shadow-lg disabled:opacity-50">{generating?<Loader2 size={16} className="animate-spin"/>:<FileText size={16}/>} {generating?"Generating…":"Generate marksheet"}</button><button type="button" onClick={()=>window.print()} disabled={!rows.length} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-[var(--ink)]/15 bg-white px-5 text-sm font-bold transition hover:-translate-y-0.5 hover:shadow-md disabled:opacity-40"><Printer size={16}/> Print / Save PDF</button>{rows.length>0&&<span className={`inline-flex items-center gap-2 rounded-full px-3 py-1.5 text-xs font-bold ${genderComplete?"bg-emerald-50 text-emerald-700":"bg-[var(--gold)]/10 text-[var(--gold)]"}`}>{genderComplete?<Check size={14}/>:<Users size={14}/>} {genderComplete?"Gender complete":"Set M/F for each learner"}</span>}</div>{message&&<p role="status" className="mt-4 rounded-xl bg-[var(--gold)]/10 px-4 py-3 text-sm font-semibold">{message}</p>}</div></section>
    {classId&&<section className="marksheet-print overflow-x-auto rounded-none bg-white p-3 sm:p-6 print:p-0"><header><h2 className="text-2xl font-black uppercase">MENWE PRIMARY SCHOOL</h2><p className="text-xs font-semibold">P.O. BOX 19, KIONYO, MERU | Email: menwejuniorss23@gmail.com</p><h3 className="mt-2 text-lg font-black uppercase">{text(titleClass?.name,"CLASS")} {text(selectedTerm?.name,"TERM")} PERFORMANCE MARKSHEET YEAR {text(years.find(y=>String(y.id)===yearId)?.name,"2026")}</h3><p className="mt-1 text-[10px] font-semibold">Assessments: {examLabel}</p></header><table className="mt-4 w-full min-w-[1100px] border-collapse text-[10px] print:min-w-0"><thead><tr><th rowSpan={2} className="border border-black p-1">No.</th><th rowSpan={2} className="border border-black p-1">Assessment No.</th><th rowSpan={2} className="border border-black p-1 text-left">Names</th><th rowSpan={2} className="border border-black p-1">Gender</th>{subjectPairs.map(s=><th key={text(s.id)} colSpan={2} className="border border-black p-1">{text(s.code)||text(s.name)}</th>)}<th rowSpan={2} className="border border-black p-1">TOTAL MARKS</th><th rowSpan={2} className="border border-black p-1">PERF LEVEL</th></tr><tr>{subjectPairs.map(s=><Fragment key={`head-${text(s.id)}`}><th className="border border-black p-1">SC</th><th className="border border-black p-1">L</th></Fragment>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={text(r.id)}><td className="border border-black p-1 text-center">{i+1}</td><td className="border border-black p-1 font-semibold">{text(r.admission_number)}</td><td className="border border-black p-1 font-semibold">{nameOf(r)}</td><td className="border border-black p-1 text-center"><select aria-label={`Gender for ${nameOf(r)}`} value={genderCode(r.gender)} disabled={savingGenderId===String(r.id)} onChange={e=>void saveGender(String(r.id),e.target.value)} className="mx-auto h-7 w-9 rounded-md border border-[var(--gold)]/30 bg-[var(--gold)]/10 px-0 text-center text-xs font-black text-[var(--ink)] outline-none focus:ring-2 focus:ring-[var(--gold)]"><option value="">—</option><option value="M">M</option><option value="F">F</option></select></td>{subjectPairs.map(s=><Fragment key={`${text(s.id)}-${i}`}><td className="border border-black p-1 text-center">{scoreFor(r,text(s.id),s.scId)}</td><td className="border border-black p-1 text-center">{scoreFor(r,text(s.id),s.lId)}</td></Fragment>)}<td className="border border-black p-1 text-center font-bold">{r.results?.length?totalFor(r):""}</td><td className="border border-black p-1 text-center font-bold">{r.results?.map((x:Row)=>x.grade).filter(Boolean).join(" / ")||""}</td></tr>)}</tbody></table><div className="mt-3 flex flex-wrap items-center justify-between gap-2 text-[9px] text-[var(--ink)]/55"><p>SC/L columns use persisted exam results whose assessment name/type identifies the corresponding component. No grading scale or marks are invented by this report.</p><p className="font-semibold">Gender: M = Male · F = Female</p></div></section>}
  </main></PortalLayout>;
}