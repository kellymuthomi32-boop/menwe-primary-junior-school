import { useEffect, useMemo, useState } from "react";
import { BookOpen, CalendarDays, CheckCircle2, ClipboardList, LibraryBig, Plus, RotateCcw, Search, TriangleAlert, Users } from "lucide-react";
import { getSupabase } from "@/lib/supabase";
import { isAdministrator, useSchoolAuth, type AppRole } from "@/contexts/SupabaseAuthContext";

type Book={id:string;accession_number:string;isbn:string|null;title:string;author:string|null;category:string;shelf_location:string|null;total_copies:number;available_copies:number;status:string;school_code?:string|null;lost_copies?:number;damaged_copies?:number;condition?:string};
type Loan={id:string;book_id:string;student_id:string;issued_at:string;due_at:string;returned_at:string|null;issue_date?:string|null;return_date?:string|null;status?:string;replacement_required?:boolean;replacement_received?:boolean;fine_amount:number;book?:{title:string;accession_number:string;school_code?:string|null}|null;student?:{first_name:string;last_name:string;admission_number:string}|null};
const staffRoles=["SUPER_ADMIN","ADMIN","HEAD_OF_INSTITUTION","DEPUTY_HOI","TEACHER"];
const adminRoles=["SUPER_ADMIN","ADMIN","HEAD_OF_INSTITUTION","DEPUTY_HOI"];
const today=()=>new Date().toISOString().slice(0,10);
export default function LibraryPage(){
 const {profile}=useSchoolAuth(); const role=profile?.role as AppRole|undefined; const admin=isAdministrator(role as any); const staff=!!role&&staffRoles.includes(role);
 const [tab,setTab]=useState<"catalogue"|"loans"|"replacements"|"register">("catalogue"); const [books,setBooks]=useState<Book[]>([]); const [loans,setLoans]=useState<Loan[]>([]); const [students,setStudents]=useState<any[]>([]); const [q,setQ]=useState(""); const [loading,setLoading]=useState(true); const [error,setError]=useState(""); const [message,setMessage]=useState(""); const [showAdd,setShowAdd]=useState(false); const [form,setForm]=useState({accession_number:"",school_code:"",title:"",author:"",isbn:"",category:"Textbook",shelf_location:"",total_copies:1}); const [checkout,setCheckout]=useState<Book|null>(null); const [studentId,setStudentId]=useState(""); const [issueDate,setIssueDate]=useState(today()); const [returnDate,setReturnDate]=useState(""); const [saving,setSaving]=useState(false);
 const load=async()=>{setLoading(true);setError("");const s=getSupabase();const [b,l,st]=await Promise.all([s.from("library_books").select("*").order("title"),s.from("library_loans").select("id,book_id,student_id,issued_at,due_at,returned_at,fine_amount,fine_paid,issue_date,return_date,status,replacement_required,replacement_received,book:library_books(title,accession_number,school_code),student:students(first_name,last_name,admission_number)").order("issued_at",{ascending:false}),staff?s.from("students").select("id,first_name,last_name,admission_number").is("deleted_at",null).order("first_name"):Promise.resolve({data:[] as any[],error:null})]);if(b.error||l.error){setError((b.error||l.error)?.message||"Unable to load library")}else{setBooks(b.data||[]);setLoans((l.data||[]) as any);setStudents(st.data||[])}setLoading(false)};
 useEffect(()=>{void load()},[]);
 const filtered=useMemo(()=>books.filter(b=>`${b.title} ${b.author||""} ${b.accession_number} ${b.school_code||""} ${b.category}`.toLowerCase().includes(q.toLowerCase())),[books,q]);
 const active=loans.filter(l=>!l.returned_at&&l.status!=="RETURNED"); const overdue=active.filter(l=>(l.return_date||l.due_at?.slice(0,10))<today()); const lost=loans.filter(l=>l.status==="LOST"||l.replacement_required); const total=books.reduce((n,b)=>n+Number(b.total_copies||0),0); const available=books.reduce((n,b)=>n+Number(b.available_copies||0),0); const lostStock=books.reduce((n,b)=>n+Number(b.lost_copies||0),0);
 const addBook=async()=>{if(!form.title.trim()||!form.accession_number.trim())return;setSaving(true);setError("");const total=Math.max(1,Number(form.total_copies));const {error:e}=await getSupabase().from("library_books").insert({...form,author:form.author||null,isbn:form.isbn||null,shelf_location:form.shelf_location||null,total_copies:total,available_copies:total,lost_copies:0,damaged_copies:0,created_by:profile?.id});if(e)setError(e.message);else{setShowAdd(false);setMessage("Textbook added to catalogue.");setForm({accession_number:"",school_code:"",title:"",author:"",isbn:"",category:"Textbook",shelf_location:"",total_copies:1});await load()}setSaving(false)};
 const issue=async()=>{if(!checkout||!studentId||!returnDate)return;setSaving(true);setError("");const {error:e}=await getSupabase().rpc("library_checkout_book",{p_book_id:checkout.id,p_student_id:studentId,p_due_at:new Date(`${returnDate}T23:59:59`).toISOString()});if(e)setError(e.message);else{setCheckout(null);setStudentId("");setReturnDate("");setIssueDate(today());setMessage(`Issued ${checkout.title} to learner.`);await load()}setSaving(false)};
 const markReturned=async(l:Loan)=>{if(!confirm("Confirm that this textbook has been returned?"))return;setSaving(true);const {error:e}=await getSupabase().rpc("library_return_book",{p_loan_id:l.id,p_fine_amount:0});if(e)setError(e.message);else{setMessage("Textbook returned. Available stock updated.");await load()}setSaving(false)};
 const markLost=async(l:Loan)=>{if(!confirm("Mark this textbook as LOST? The learner will appear on the replacement register."))return;setSaving(true);setError("");const {error:e}=await getSupabase().from("library_loans").update({returned_at:new Date().toISOString(),returned_by:profile?.id,status:"LOST",replacement_required:true,lost_at:new Date().toISOString()}).eq("id",l.id);if(e){setError(e.message);setSaving(false);return}await getSupabase().from("library_books").update({available_copies:Math.max(0,(books.find(b=>b.id===l.book_id)?.available_copies||0)),lost_copies:Number(books.find(b=>b.id===l.book_id)?.lost_copies||0)+1}).eq("id",l.book_id);await getSupabase().from("library_replacement_records").insert({loan_id:l.id,book_id:l.book_id,learner_id:l.student_id,replacement_type:"BUY_NEW",expected_cost:0,notes:"Lost textbook; new replacement required."});setMessage("Lost textbook recorded. It is now on the BUY NEW replacement register.");await load();setSaving(false)};
 const replacementReceived=async(l:Loan)=>{setSaving(true);const b=books.find(x=>x.id===l.book_id);if(b){await getSupabase().from("library_books").update({total_copies:Number(b.total_copies||0)+1,available_copies:Number(b.available_copies||0)+1,lost_copies:Math.max(0,Number(b.lost_copies||0)-1)}).eq("id",b.id)}await getSupabase().from("library_loans").update({status:"REPLACED",replacement_received:true}).eq("id",l.id);await getSupabase().from("library_replacement_records").update({resolved_at:new Date().toISOString(),resolved_by:profile?.id}).eq("loan_id",l.id).is("resolved_at",null);setMessage("Replacement received and stock restored.");await load();setSaving(false)};
 return <main className="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8"><div className="flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><div className="flex items-center gap-3"><span className="grid h-12 w-12 place-items-center rounded-xl bg-[#061229]/5 text-[#061229]"><LibraryBig size={23}/></span><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#D89B28]">School library</p><h1 className="text-2xl font-black tracking-tight text-[#061229] sm:text-3xl">Textbook control centre</h1></div></div><p className="mt-3 max-w-3xl text-sm leading-6 text-slate-600">A complete register for school textbooks: catalogue, school codes, learner issues, issue dates, return dates, overdue books, lost books and replacements to buy.</p></div>{admin&&<button onClick={()=>setShowAdd(true)} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl bg-[#061229] px-4 text-sm font-bold text-white shadow-md transition hover:-translate-y-0.5"><Plus size={17}/>Add textbook</button>}</div>
 {message&&<div className="mt-4 rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-sm font-semibold text-emerald-800">{message}</div>}{error&&<div className="mt-4 rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</div>}
 <div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-5"><Stat icon={BookOpen} label="Titles" value={books.length}/><Stat icon={CheckCircle2} label="Available copies" value={available}/><Stat icon={ClipboardList} label="Issued" value={active.length}/><Stat icon={TriangleAlert} label="Overdue" value={overdue.length}/><Stat icon={TriangleAlert} label="Lost / replace" value={lost.length+lostStock}/></div>
 <div className="mt-6 flex flex-wrap gap-2">{([["catalogue","Catalogue"],["loans","Issue & return"],["replacements","Replacement register"],["register","Stock summary"]] as const).map(([k,label])=><button key={k} onClick={()=>setTab(k)} className={`rounded-xl px-4 py-2.5 text-sm font-bold ${tab===k?"bg-[#061229] text-white":"border border-slate-200 bg-white text-slate-600"}`}>{label}</button>)}</div>
 {tab!=="register"&&<div className="mt-4 relative"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search book name, school code, author, category…" className="h-11 w-full rounded-xl border border-slate-200 bg-white pl-10 pr-4 text-sm outline-none focus:border-[#D89B28]"/></div>}
 {loading?<div className="py-16 text-center text-sm text-slate-500">Loading library…</div>:tab==="catalogue"?<div className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{filtered.map(b=><article key={b.id} className="rounded-2xl border border-gray-100 bg-white p-5 shadow-md transition-all duration-300 hover:-translate-y-1.5 hover:shadow-xl"><div className="flex items-start justify-between gap-3"><span className="rounded-full bg-[#D89B28]/10 px-3 py-1 text-xs font-bold uppercase text-[#D89B28]">{b.category}</span><span className="text-xs font-bold text-emerald-600">{b.available_copies}/{b.total_copies} available</span></div><h2 className="mt-4 text-lg font-black text-[#061229]">{b.title}</h2><p className="mt-1 text-sm text-slate-500">{b.author||"Author not recorded"}</p><div className="mt-4 grid gap-1 text-xs text-slate-500"><p>School code: <b className="text-slate-700">{b.school_code||"—"}</b></p><p>Accession: <b className="text-slate-700">{b.accession_number}</b></p>{b.shelf_location&&<p>Shelf: {b.shelf_location}</p>}{Number(b.lost_copies||0)>0&&<p className="font-bold text-red-600">Lost copies: {b.lost_copies}</p>}</div>{staff&&<button disabled={b.available_copies<1||b.status!=="AVAILABLE"} onClick={()=>setCheckout(b)} className="mt-5 min-h-10 w-full rounded-xl bg-[#061229] text-sm font-bold text-white disabled:cursor-not-allowed disabled:opacity-40">Issue to learner</button>}</article>)}{filtered.length===0&&<div className="md:col-span-3 rounded-2xl border border-dashed border-slate-300 p-10 text-center text-sm text-slate-500">No textbooks match your search.</div>}</div>:tab==="loans"?<div className="mt-5 overflow-hidden rounded-2xl border border-gray-100 bg-white shadow-md"><div className="overflow-x-auto"><table className="min-w-[950px] w-full text-left text-sm"><thead className="bg-[#061229] text-xs uppercase tracking-wider text-white"><tr><th className="px-4 py-3">Textbook</th><th className="px-4 py-3">Learner</th><th className="px-4 py-3">Issue date</th><th className="px-4 py-3">Return date</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Action</th></tr></thead><tbody>{active.map(l=>{const late=(l.return_date||l.due_at?.slice(0,10))<today();return <tr key={l.id} className="border-t border-slate-100"><td className="px-4 py-4 font-bold text-[#061229]">{l.book?.title}<div className="text-xs font-normal text-slate-400">{l.book?.school_code||l.book?.accession_number}</div></td><td className="px-4 py-4">{l.student?.first_name} {l.student?.last_name}<div className="text-xs text-slate-400">{l.student?.admission_number}</div></td><td className="px-4 py-4">{l.issue_date||l.issued_at?.slice(0,10)}</td><td className="px-4 py-4">{l.return_date||l.due_at?.slice(0,10)}</td><td className="px-4 py-4"><span className={late?"font-bold text-red-600":"font-bold text-emerald-600"}>{late?"OVERDUE":"ISSUED"}</span></td><td className="px-4 py-4"><div className="flex gap-2"><button disabled={saving} onClick={()=>void markReturned(l)} className="rounded-lg border border-emerald-200 px-3 py-2 text-xs font-bold text-emerald-700"><RotateCcw size={14} className="mr-1 inline"/>Returned</button><button disabled={saving} onClick={()=>void markLost(l)} className="rounded-lg border border-red-200 px-3 py-2 text-xs font-bold text-red-700"><TriangleAlert size={14} className="mr-1 inline"/>Lost</button></div></td></tr>})}</tbody></table>{active.length===0&&<div className="p-10 text-center text-sm text-slate-500">No active textbook issues.</div>}</div></div>:tab==="replacements"?<div className="mt-5 grid gap-4">{lost.map(l=><article key={l.id} className="rounded-2xl border border-red-100 bg-white p-5 shadow-md"><div className="flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div><span className="rounded-full bg-red-50 px-3 py-1 text-xs font-bold uppercase text-red-700">BUY NEW</span><h2 className="mt-3 font-black text-[#061229]">{l.book?.title}</h2><p className="mt-1 text-sm text-slate-600">Learner: {l.student?.first_name} {l.student?.last_name} · {l.student?.admission_number}</p><p className="mt-1 text-xs text-slate-500">School code: {l.book?.school_code||l.book?.accession_number} · Lost textbook</p></div>{canManage&&<button disabled={saving} onClick={()=>void replacementReceived(l)} className="rounded-xl bg-[#D89B28] px-4 py-2.5 text-sm font-bold text-[#061229]">Replacement received</button>}</div></article>)}{lost.length===0&&<div className="rounded-2xl border border-dashed border-slate-300 p-10 text-center text-sm text-slate-500">No textbooks currently awaiting replacement.</div>}</div>:<div className="mt-5 overflow-hidden rounded-2xl border border-gray-100 bg-white shadow-md"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr><th className="px-4 py-3">Book</th><th className="px-4 py-3">School code</th><th className="px-4 py-3">Total</th><th className="px-4 py-3">Available</th><th className="px-4 py-3">Lost</th><th className="px-4 py-3">Need to buy</th></tr></thead><tbody>{books.map(b=><tr key={b.id} className="border-t border-slate-100"><td className="px-4 py-3 font-bold">{b.title}</td><td>{b.school_code||b.accession_number}</td><td>{b.total_copies}</td><td className="font-bold text-emerald-600">{b.available_copies}</td><td className="font-bold text-red-600">{b.lost_copies||0}</td><td className="font-bold text-[#D89B28]">{b.lost_copies||0}</td></tr>)}</tbody></table></div>}
 {showAdd&&<Modal title="Add textbook to school register" onClose={()=>setShowAdd(false)}><div className="grid gap-3 sm:grid-cols-2">{([['title','Book name'],['school_code','School book code'],['accession_number','Accession number'],['author','Author'],['isbn','ISBN'],['category','Category'],['shelf_location','Shelf location']] as const).map(([k,label])=><label key={k} className="text-xs font-bold text-slate-600">{label}<input required={k==='title'||k==='accession_number'} value={(form as any)[k]} onChange={e=>setForm(f=>({...f,[k]:e.target.value}))} className="mt-1 h-11 w-full rounded-xl border border-slate-200 px-3 text-sm font-normal outline-none focus:border-[#D89B28]"/></label>)}<label className="text-xs font-bold text-slate-600">Number of copies<input type="number" min={1} value={form.total_copies} onChange={e=>setForm(f=>({...f,total_copies:Math.max(1,Number(e.target.value))}))} className="mt-1 h-11 w-full rounded-xl border border-slate-200 px-3 text-sm font-normal"/></label></div><button onClick={()=>void addBook()} disabled={saving||!form.title||!form.accession_number} className="mt-5 min-h-11 w-full rounded-xl bg-[#061229] text-sm font-bold text-white disabled:opacity-40">{saving?"Saving…":"Save textbook"}</button></Modal>}
 {checkout&&<Modal title="Issue textbook to learner" onClose={()=>setCheckout(null)}><div className="rounded-xl bg-slate-50 p-3 text-sm"><b>{checkout.title}</b><div className="mt-1 text-xs text-slate-500">School code: {checkout.school_code||checkout.accession_number}</div></div><label className="mt-4 block text-xs font-bold text-slate-600">Learner<select value={studentId} onChange={e=>setStudentId(e.target.value)} className="mt-1 h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm font-normal"><option value="">Select learner</option>{students.map(s=><option key={s.id} value={s.id}>{s.first_name} {s.last_name} · {s.admission_number}</option>)}</select></label><div className="mt-3 grid gap-3 sm:grid-cols-2"><label className="text-xs font-bold text-slate-600">Issue date<input type="date" value={issueDate} onChange={e=>setIssueDate(e.target.value)} className="mt-1 h-11 w-full rounded-xl border border-slate-200 px-3 text-sm font-normal"/></label><label className="text-xs font-bold text-slate-600">Return date<input type="date" required value={returnDate} onChange={e=>setReturnDate(e.target.value)} className="mt-1 h-11 w-full rounded-xl border border-slate-200 px-3 text-sm font-normal"/></label></div><button onClick={()=>void issue()} disabled={saving||!studentId||!returnDate} className="mt-5 min-h-11 w-full rounded-xl bg-[#061229] text-sm font-bold text-white disabled:opacity-40">{saving?"Issuing…":"Issue textbook"}</button></Modal>}
 </main>
}
function Stat({icon:Icon,label,value}:{icon:any;label:string;value:number}){return <div className="rounded-2xl border border-gray-100 bg-white p-4 shadow-md"><div className="flex items-center justify-between"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#061229]/5 text-[#061229]"><Icon size={18}/></span><span className="text-2xl font-black text-[#061229]">{value}</span></div><p className="mt-3 text-xs font-bold uppercase tracking-wider text-slate-400">{label}</p></div>}
function Modal({title,onClose,children}:{title:string;onClose:()=>void;children:any}){return <div className="fixed inset-0 z-50 grid place-items-center bg-[#061229]/55 p-4"><div className="w-full max-w-lg rounded-2xl bg-white p-5 shadow-2xl"><div className="flex items-center justify-between"><h2 className="text-lg font-black text-[#061229]">{title}</h2><button onClick={onClose} className="rounded-lg px-3 py-2 text-sm font-bold text-slate-500 hover:bg-slate-100">Close</button></div><div className="mt-4">{children}</div></div></div>}
